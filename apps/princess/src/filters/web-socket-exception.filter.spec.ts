import { WebSocketExceptionFilter } from './web-socket-exception.filter'

describe('WebSocketExceptionFilter', () => {
  it('should be defined', () => {
    expect(new WebSocketExceptionFilter()).toBeDefined()
  })
})
